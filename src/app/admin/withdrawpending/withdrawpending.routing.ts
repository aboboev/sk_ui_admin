import {Routes, RouterModule} from '@angular/router';
import {WithdrawPendingComponent} from "./withdrawpending.component";

export const WithdrawPendingRoutes: Routes = [
    {
        path: '',
        component: WithdrawPendingComponent
    }
];
